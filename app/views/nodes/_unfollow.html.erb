<%= form_for current_user.relationships.find_by_followed_id(@node), :html => { :method => :delete } do |f| %>
    <div class="actions"><%= f.submit "Leave #{@node.name}", :class => "nice small radius black button" %></div>
<% end %>

